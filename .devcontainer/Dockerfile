FROM mcr.microsoft.com/devcontainers/typescript-node:20
# Uncomment if jq isn’t present in the base image in your environment
# RUN apt-get update && apt-get install -y jq && rm -rf /var/lib/apt/lists/*
